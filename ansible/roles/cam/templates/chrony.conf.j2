# Chrony NTP configuration for SyncDrive camera
# Sync directly to internet NTP with aggressive polling

pool pool.ntp.org iburst minpoll 4 maxpoll 6
makestep 1 -1
driftfile /var/lib/chrony/chrony.drift
logdir /var/log/chrony
